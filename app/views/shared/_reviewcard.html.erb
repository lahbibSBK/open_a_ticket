<div class="reviewcard">
  <div class="reviewcard-stars">
    <% if review.blank? %>
      <p>No review yet</p>
    <% else %>
      <% review.rating.to_i.times do |i| %>
        <i class="fas fa-star"></i>
      <% end %>
      <% (5 - review.rating.to_i).times do |i| %>
        <i class="far fa-star grey"></i>
        <% end %>
    <% end %>
  </div>

  <div class="reviewcard-content">
   <%= review.description %>>
  </div>

  <div class="reviewcard-avatar">
    <img src=<%= review.ticket.alumni.pic_url%>  class="avatar-xsmall"></li></p>
  </div>

</div>
