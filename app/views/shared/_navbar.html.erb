<nav class="navbar sticky-top bg-white mb-0 flex_box space-around bottom-shadow">
    <div class="flex_box width-90 space-between marginx-auto">

      <div class="logo_part">
        <a id="logo" href="/" text-gray-hover>
          <i class="fas fa-ticket-alt font-30 text-gray"></i>
        </a>
      </div>

      <div class="flex_box space-between">

          <% if current_user %>
            <div class="flex_box space-between">

                <a href="/tickets/new" class="btn-open-s font-20 text-white ml-2 <%= 'hidden' if current_page?(root_path) && current_user %>">Open a ticket</a>

                <a href="/mentor_profils/new" class="btn-open-s font-20 text-white ml-2 hidden-xs" id="open_btn">Become a Mentor</a>

                  <div class="dropdown">
                    <img src=<%= current_user.pic_url %> class="avatar ml-2" id="navbar-wagon-menu" data-toggle="dropdown">
                    <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
                      <li><%= link_to "Dashboard", dashboard_path, class: "dropdown-link" %></li>
                      <li><%= link_to "Settings", edit_user_registration_path, class: "dropdown-link" %></li>
                      <li><%= link_to "Log out", destroy_user_session_path, class: "dropdown-link", method: "delete", data: { confirm: "Are you sure you want to log out ?" } %></li>
                    </ul>
                  </div>

            </div>
          <% end %>

          <a href="/mentor_profils/new" class="btn-open-s font-20 text-white ml-2 <%= "hidden" if current_user || current_page?("users/sign_in") %>">Sign in</a>

        </div>

      </div>

    </div>
</nav>

