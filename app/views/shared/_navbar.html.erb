<nav class="navbar sticky-top bg-white mb-0 flex_box space-around bottom-shadow">
    <div class="flex_box width-90 space-between marginx-auto">

      <div class="logo_part">
        <a id="logo" href="/" text-gray-hover>
          <i class="fas fa-ticket-alt font-30 text-gray"></i>
        </a>
      </div>

      <div class="flex_box space-between">

        <a href="/dashboard" class="btn-open btn-primary text-white ml-2 hidden-xs <%= "hidden" if current_user %> <%= "visible" if !current_user %>">Sign in</a>
          <% if current_user %>
            <div class="flex_box space-between">

              <%= link_to 'Open a ticket', new_ticket_path, class: 'navbar-link' %>
              <%= link_to 'Become a Mentor', new_mentor_profil_path, class: 'navbar-link' %>
              <%= link_to 'My tickets', tickets_path, class: 'navbar-link' %>

              <%= image_tag current_user.pic_url, class: 'avatar ml-2', id: 'user-control' %>

              <div class="open-dropdown hidden">
                <%= link_to "Dashboard", dashboard_path %>
                <%= link_to "Settings", edit_user_registration_path %>
                <%= link_to "Log out", destroy_user_session_path, method: "delete", data: { confirm: "Are you sure you want to log out ?" } %>
              </div>

            </div>
          <% end %>
          <% if !current_user %>
            <span id="user-control"></span>
            <span class="open-dropdown"></span>
          <% end %>
        </div>

      </div>

    </div>
</nav>

